# 修正内容の確認 (Walkthrough) - レイアウトと要約の調整

## 概要
ユーザーのフィードバックに基づき、情報の表示順序を「タイトル → 画像 → 要約 → インサイト」に変更し、ニュースの要約文を約200文字のボリュームに調整しました。

## 修正内容
1.  **要約の長尺化**:
    - `src/insightGenerator.js` 内の Gemini プロンプトを更新し、`translatedSnippet` (要約) の文字数指定を「200文字程度」に変更しました。
2.  **表示順序の変更**:
    - `src/templateGenerator.js` を修正し、メールおよび Web ギャラリーの構成要素を下図の順序に並び替えました：
        1. タイトル
        2. メイン画像
        3. 要約 (約200文字)
        4. AIインサイト

## 検証結果
- レイアウト順序が意図通りに変更されていることをテンプレートコード上で確認しました。
- 各ソースコードの変更を GitHub にプッシュしました。

## 今後のメンテナンス
- 今後、さらに要約を長く、あるいは短くしたい場合は、`src/insightGenerator.js` のプロンプト内の文字数指定を変更してください。
